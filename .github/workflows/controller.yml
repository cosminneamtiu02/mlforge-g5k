name: PR Controller

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow triggered on PR #${{ github.event.pull_request.number }}"
